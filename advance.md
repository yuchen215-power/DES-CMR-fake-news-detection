# 创新点总结


### 可导稀疏证据路由机制

传统动态证据选择通常依赖离散的硬 `top-k` 筛选，证据选择过程难以与下游分类目标协同优化。为此，本文设计了可导稀疏证据路由机制：先计算声明与候选证据之间的匹配分数，再通过稀疏归一化函数生成证据级软路由权重，并将其作为先验注入证据级聚合过程。该方法能够实现证据选择与真实性判别的端到端联合优化，提升关键证据利用能力。

具体做了以下工作：

1. 为每条候选证据构造声明感知匹配特征，包括声明表示、证据表示、逐维乘积项以及差异项，用于更细致地描述声明与证据之间的关系。
2. 使用轻量路由网络输出每条证据的相关性分数，并通过稀疏归一化方式得到软选择权重，而不是直接做离散裁剪。
3. 将路由权重作为证据级注意力的先验，引导模型在后续聚合阶段更加聚焦于高价值证据。
4. 引入路由熵正则，避免所有注意力都集中到单条证据上，提高证据选择的稳定性。

这一模块解决的核心问题是：把“选证据”和“做分类”从两段式割裂流程，变成一个可以共同优化的整体过程。

可以概括为：

> 提出可导稀疏证据路由机制，实现证据选择与真实性判别的联合优化。

### 来源可信度感知的证据聚合机制

事实核查任务中，不同来源的证据在可靠性上存在显著差异。为此，本文在证据级注意力中引入来源可信度先验，利用文章来源嵌入学习来源级可靠性偏置，并以残差方式注入证据聚合过程。这样一来，模型在保持文本内容建模能力的同时，可以更加有效地区分高可信与低可信来源，从而提升证据加权的合理性与最终判别稳定性。

具体做了以下工作：

1. 为每条证据保留文章来源嵌入，使模型可以显式接触证据来源信息。
2. 设计轻量可信度映射网络，将来源嵌入映射为证据级注意力偏置，而不是直接替代文本内容表示。
3. 采用小幅度残差注入方式，把来源偏置叠加到原有 evidence attention logits 上，保证来源信息起到“辅助修正”而不是“强制主导”的作用。
4. 采用零初始化策略，让模型在训练初期尽量保持原始行为，再逐步学习来源可信度模式，降低结构改动带来的不稳定风险。

这一模块的核心作用在于：让模型在“证据内容相关性”之外，再额外考虑“证据来源可靠性”，从而使证据权重分配更加贴近真实事实核查场景。

可以概括为：

> 提出来源可信度感知的证据聚合机制，使证据权重分配兼顾语义相关性与来源可靠性。

### 跨证据关系增强模块

现有证据聚合方法通常主要关注声明与单条证据之间的匹配关系，而忽视了多条证据之间的补充、重复、支持和冲突关系。为解决这一问题，本文在证据级聚合前引入跨证据关系增强模块，通过自注意力机制显式建模证据与证据之间的交互依赖，并采用门控残差方式对证据表示进行自适应更新。该模块能够在多证据联合推理场景下增强上下文一致性建模能力，提升最终聚合表示质量。

这是本文中一个相对完整的结构性扩展，具体做了以下工作：

1. 在证据级聚合之前，把每条证据的表示整理为统一的证据表示序列，使其能够进入跨证据交互模块。
2. 引入多头自注意力机制，对同一声明下的多条证据进行全局交互建模，使每条证据在更新时不仅考虑声明信息，也能感知其他证据的内容。
3. 使用 `LayerNorm + Self-Attention + FeedForward` 的结构，对证据集合进行上下文细化，使证据表示包含“单证据语义 + 多证据上下文”两部分信息。
4. 设计门控残差融合机制，在原始证据表示与交互后证据表示之间进行自适应融合，避免新模块过强干扰原有主干表示。
5. 对该模块采用近似恒等映射初始化策略，包括将部分输出层初始化为零、让门控初始偏向保留原表示，从而保证模块加入初期不会显著破坏原模型性能，训练过程中再逐步学习有效的跨证据关系模式。

从问题角度看，这一模块主要解决三个事实核查中的典型现象：

1. **证据互补**：单条证据无法完整支撑判断，多条证据合起来才构成完整事实链条。
2. **证据冗余**：多条证据表达相近，模型需要识别重复信息，避免重复证据被过度放大。
3. **证据冲突**：不同证据之间可能存在支持与反驳的对立关系，模型需要在聚合前先建模这种冲突。

从实现角度看，这一模块不是简单“再加一层注意力”，而是明确将“证据之间的关系建模”从原有的声明-证据匹配流程中单独拆出来，形成独立的多证据联合推理阶段。也就是说，模型的推理路径被扩展为：

`声明编码 -> 单证据编码 -> 证据路由 -> 跨证据关系增强 -> 证据级聚合 -> 最终分类`

这样设计的好处是，证据级 attention 最终看到的不再是彼此孤立的证据，而是已经经过多证据上下文修正后的证据表示，因此更有利于提升复杂样本上的联合判断能力。

可以概括为：

> 提出跨证据关系增强模块，在证据聚合前显式建模证据间语义依赖，提升多证据联合推理能力。

### 自适应决策边界校准机制

在事实核查任务中，模型输出的后验概率与最终离散类别决策之间往往存在偏差，固定阈值并不总是适合具体数据分布。为此，本文提出自适应决策边界校准机制，利用验证集分布信息对决策边界进行动态调整，使最终预测结果更符合当前任务的数据特征。与固定阈值判别相比，该方法能够缓解类别预测偏置，提高模型决策的稳定性与泛化能力，同时无需引入额外训练参数或增加模型训练开销。

具体做了以下工作：

1. 不再直接采用固定 `argmax` 或固定 `0.5` 作为最终分类边界。
2. 在验证集上根据模型输出概率搜索更合适的决策阈值，使最终离散分类结果更符合当前数据分布。
3. 将在验证集上得到的最优阈值迁移到测试阶段，用于最终预测与结果输出。
4. 将阈值搜索纳入训练日志和最终结果报告，使模型性能分析不仅关注表示学习，也关注后验决策过程。

这一部分工作的价值在于：它优化的不是编码能力，而是“概率到标签”的映射过程，从而提升最终决策可靠性。

可以概括为：

> 提出自适应决策边界校准机制，提升模型后验概率到最终类别映射过程的稳定性与可靠性。
